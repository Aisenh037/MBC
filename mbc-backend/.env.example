<<<<<<< HEAD
# Server
NODE_ENV=development
PORT=5000
HOST=0.0.0.0
LOG_LEVEL=info

# Database
MONGO_URI=
USE_IN_MEMORY_DB=false

# Security
JWT_SECRET=change_me
CORS_ORIGIN=http://localhost:5173

# Admin seeding
SEED_ADMIN=false
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change_me

# Email (SMTP)
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_USER=
EMAIL_PASS=
FROM_NAME=MBC Department
FROM_EMAIL=noreply@example.com
=======
# .env.example
# Copy this file to .env and fill in your actual values.
# DO NOT COMMIT THE .env FILE TO VERSION CONTROL.

# ----------------------------------
# ENVIRONMENT
# ----------------------------------
# Set to 'development' or 'production'
NODE_ENV=development

# ----------------------------------
# SERVER & DATABASE
# ----------------------------------
PORT=5000
# Your MongoDB Atlas connection string
MONGO_URI=
# Comma-separated list of allowed frontend URLs for CORS
CORS_ORIGIN=http://localhost:5173

# ----------------------------------
# AUTHENTICATION & SECURITY
# ----------------------------------
# ✨ IMPORTANT: Generate a strong, random string for your JWT secret.
# You can use an online generator or a command like: openssl rand -base64 32
JWT_SECRET=
JWT_EXPIRE=30d

# Initial super admin credentials
ADMIN_EMAIL=admin@example.com
# ✨ IMPORTANT: Use a strong, unique password for the admin account in production.
ADMIN_PASSWORD=

# ----------------------------------
# RATE LIMITING & FILE UPLOADS
# ----------------------------------
RATE_LIMIT_WINDOW=900000
RATE_LIMIT_MAX=100
MAX_FILE_UPLOAD=10485760

# ----------------------------------
# EMAIL SERVICE
# ----------------------------------
# For production, use a dedicated email service like SendGrid, Mailgun, etc.
# These values should be kept secret.
FROM_EMAIL=donotreply@example.com
FROM_NAME=MBC Department Portal
SENDGRID_API_KEY=
>>>>>>> c806d27 (updated env file)
